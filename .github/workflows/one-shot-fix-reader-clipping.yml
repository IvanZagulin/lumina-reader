name: Validate reader stats patch

on:
  push:
    branches:
      - automation/fix-reader-page-clipping

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: true

      - name: Apply requested changes
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(text, old, new, label):
              if old not in text:
                  raise SystemExit(f"Anchor not found: {label}")
              return text.replace(old, new, 1)

          reader = Path("app/src/main/java/com/lumina/reader/ui/reader/ReaderScreen.kt")
          text = reader.read_text(encoding="utf-8")
          old = '''    // Immersive system bars toggle
    DisposableEffect(showControls) {
        val window = (context as? Activity)?.window
        if (window != null) {
            val insetsController = WindowCompat.getInsetsController(window, view)
            insetsController.systemBarsBehavior = WindowInsetsControllerCompat.BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE
            if (showControls) {
                insetsController.show(WindowInsetsCompat.Type.systemBars())
            } else {
                insetsController.hide(WindowInsetsCompat.Type.systemBars())
            }
        }
        onDispose {
            val window = (context as? Activity)?.window
            if (window != null) {
                val insetsController = WindowCompat.getInsetsController(window, view)
                insetsController.show(WindowInsetsCompat.Type.systemBars())
            }
        }
    }'''
          new = '''    // Keep the status bar visible in reading mode so the clock, battery and
    // system indicators remain available. Only the navigation bar stays immersive
    // while reader controls are hidden.
    DisposableEffect(showControls, settings.theme) {
        val window = (context as? Activity)?.window
        if (window != null) {
            val insetsController = WindowCompat.getInsetsController(window, view)
            insetsController.systemBarsBehavior = WindowInsetsControllerCompat.BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE
            insetsController.isAppearanceLightStatusBars = when (settings.theme.name) {
                "SEPIA", "CREAM", "LIGHT" -> true
                else -> false
            }
            insetsController.show(WindowInsetsCompat.Type.statusBars())
            if (showControls) {
                insetsController.show(WindowInsetsCompat.Type.navigationBars())
            } else {
                insetsController.hide(WindowInsetsCompat.Type.navigationBars())
            }
        }
        onDispose {
            val window = (context as? Activity)?.window
            if (window != null) {
                val insetsController = WindowCompat.getInsetsController(window, view)
                insetsController.show(WindowInsetsCompat.Type.systemBars())
            }
        }
    }'''
          reader.write_text(replace_once(text, old, new, "reader system bars"), encoding="utf-8")

          stats = Path("app/src/main/java/com/lumina/reader/ui/stats/StatsScreen.kt")
          text = stats.read_text(encoding="utf-8")
          old = '''                item { SectionTitle("Библиотека", Icons.AutoMirrored.Filled.MenuBook) }

                item { LibraryCard(state) }

                if (state.mostReadBooks.isNotEmpty()) {
                    item { SectionTitle("Больше всего читали", Icons.Default.EmojiEvents) }

                    itemsIndexed(
                        items = state.mostReadBooks,
                        key = { _, item -> item.bookId }
                    ) { index, book ->
                        TopBookCard(
                            rank = index + 1,
                            book = book,
                            maximumDuration = state.mostReadBooks.maxOf { it.durationSeconds }
                        )
                    }
                }



                if (state.ignoredSessionCount > 0) {'''
          new = '''                item { SectionTitle("Ещё немного цифр", Icons.Default.History) }

                item {
                    Row(
                        modifier = Modifier.fillMaxWidth(),
                        horizontalArrangement = Arrangement.spacedBy(10.dp)
                    ) {
                        InsightCard(
                            title = "Всего сессий",
                            value = formatNumber(state.allTime.sessionCount.toLong()),
                            icon = Icons.Default.History,
                            color = StatsBlue,
                            modifier = Modifier.weight(1f)
                        )
                        InsightCard(
                            title = "Примерно страниц",
                            value = formatNumber(state.allTime.estimatedPages),
                            icon = Icons.Default.AutoStories,
                            color = StatsPurple,
                            modifier = Modifier.weight(1f)
                        )
                    }
                }

                item {
                    Row(
                        modifier = Modifier.fillMaxWidth(),
                        horizontalArrangement = Arrangement.spacedBy(10.dp)
                    ) {
                        InsightCard(
                            title = "Рекорд серии",
                            value = "${state.bestStreakDays} ${dayWord(state.bestStreakDays)}",
                            icon = Icons.Default.LocalFireDepartment,
                            color = StatsOrange,
                            modifier = Modifier.weight(1f)
                        )
                        InsightCard(
                            title = "Книг открыто",
                            value = formatNumber(state.allTime.bookCount.toLong()),
                            icon = Icons.AutoMirrored.Filled.MenuBook,
                            color = StatsGreen,
                            modifier = Modifier.weight(1f)
                        )
                    }
                }

                item { SectionTitle("Библиотека", Icons.AutoMirrored.Filled.MenuBook) }
                item { LibraryCard(state) }

                if (state.recentSessions.isNotEmpty()) {
                    item { SectionTitle("Последние чтения", Icons.Default.History) }
                    itemsIndexed(
                        items = state.recentSessions.take(5),
                        key = { _, session -> session.id }
                    ) { _, session ->
                        RecentSessionCard(session)
                    }
                }

                if (state.ignoredSessionCount > 0) {'''
          stats.write_text(replace_once(text, old, new, "stats most-read section"), encoding="utf-8")
          PY
          git diff --check

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Build and test
        id: build
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          chmod +x gradlew
          ./gradlew testDebugUnitTest assembleDebug --no-daemon 2>&1 | tee /tmp/validation-build.log

      - name: Publish validation result
        shell: bash
        env:
          BUILD_OUTCOME: ${{ steps.build.outcome }}
        run: |
          {
            echo "build=$BUILD_OUTCOME"
            echo
            if [ -f /tmp/validation-build.log ]; then
              tail -n 500 /tmp/validation-build.log
            fi
          } > /tmp/ci-validation.txt
          git reset --hard HEAD
          git clean -fd
          git checkout -B automation/ci-validation-result
          cp /tmp/ci-validation.txt ci-validation.txt
          git add ci-validation.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Record reader stats validation result"
          git push origin HEAD:refs/heads/automation/ci-validation-result --force

      - name: Fail workflow when build failed
        if: steps.build.outcome != 'success'
        run: exit 1
