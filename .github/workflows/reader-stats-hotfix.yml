name: Reader stats hotfix deployment

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/reader-stats-hotfix.yml
      - .github/reader-stats-trigger

permissions:
  contents: write

concurrency:
  group: reader-stats-hotfix
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: true

      - name: Apply requested changes
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(text, old, new, label):
              if old not in text:
                  raise SystemExit(f"Anchor not found: {label}")
              return text.replace(old, new, 1)

          reader = Path("app/src/main/java/com/lumina/reader/ui/reader/ReaderScreen.kt")
          text = reader.read_text(encoding="utf-8")
          old = '''    // Immersive system bars toggle
    DisposableEffect(showControls) {
        val window = (context as? Activity)?.window
        if (window != null) {
            val insetsController = WindowCompat.getInsetsController(window, view)
            insetsController.systemBarsBehavior = WindowInsetsControllerCompat.BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE
            if (showControls) {
                insetsController.show(WindowInsetsCompat.Type.systemBars())
            } else {
                insetsController.hide(WindowInsetsCompat.Type.systemBars())
            }
        }
        onDispose {
            val window = (context as? Activity)?.window
            if (window != null) {
                val insetsController = WindowCompat.getInsetsController(window, view)
                insetsController.show(WindowInsetsCompat.Type.systemBars())
            }
        }
    }'''
          new = '''    // Keep the Android status bar visible in reading mode so the clock,
    // battery level and system indicators are always available. Only the
    // navigation bar stays immersive while reader controls are hidden.
    DisposableEffect(showControls, settings.theme) {
        val window = (context as? Activity)?.window
        if (window != null) {
            val insetsController = WindowCompat.getInsetsController(window, view)
            insetsController.systemBarsBehavior = WindowInsetsControllerCompat.BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE
            insetsController.isAppearanceLightStatusBars = when (settings.theme.name) {
                "SEPIA", "CREAM", "LIGHT" -> true
                else -> false
            }
            insetsController.show(WindowInsetsCompat.Type.statusBars())
            if (showControls) {
                insetsController.show(WindowInsetsCompat.Type.navigationBars())
            } else {
                insetsController.hide(WindowInsetsCompat.Type.navigationBars())
            }
        }
        onDispose {
            val window = (context as? Activity)?.window
            if (window != null) {
                val insetsController = WindowCompat.getInsetsController(window, view)
                insetsController.show(WindowInsetsCompat.Type.systemBars())
            }
        }
    }'''
          reader.write_text(replace_once(text, old, new, "reader system bars"), encoding="utf-8")

          stats = Path("app/src/main/java/com/lumina/reader/ui/stats/StatsScreen.kt")
          text = stats.read_text(encoding="utf-8")
          old = '''                item { SectionTitle("Библиотека", Icons.AutoMirrored.Filled.MenuBook) }

                item { LibraryCard(state) }

                if (state.mostReadBooks.isNotEmpty()) {
                    item { SectionTitle("Больше всего читали", Icons.Default.EmojiEvents) }

                    itemsIndexed(
                        items = state.mostReadBooks,
                        key = { _, item -> item.bookId }
                    ) { index, book ->
                        TopBookCard(
                            rank = index + 1,
                            book = book,
                            maximumDuration = state.mostReadBooks.maxOf { it.durationSeconds }
                        )
                    }
                }



                if (state.ignoredSessionCount > 0) {'''
          new = '''                item { SectionTitle("Ещё немного цифр", Icons.Default.History) }

                item {
                    Row(
                        modifier = Modifier.fillMaxWidth(),
                        horizontalArrangement = Arrangement.spacedBy(10.dp)
                    ) {
                        InsightCard(
                            title = "Всего сессий",
                            value = formatNumber(state.allTime.sessionCount.toLong()),
                            icon = Icons.Default.History,
                            color = StatsBlue,
                            modifier = Modifier.weight(1f)
                        )
                        InsightCard(
                            title = "Примерно страниц",
                            value = formatNumber(state.allTime.estimatedPages),
                            icon = Icons.Default.AutoStories,
                            color = StatsPurple,
                            modifier = Modifier.weight(1f)
                        )
                    }
                }

                item {
                    Row(
                        modifier = Modifier.fillMaxWidth(),
                        horizontalArrangement = Arrangement.spacedBy(10.dp)
                    ) {
                        InsightCard(
                            title = "Рекорд серии",
                            value = "${state.bestStreakDays} ${dayWord(state.bestStreakDays)}",
                            icon = Icons.Default.LocalFireDepartment,
                            color = StatsOrange,
                            modifier = Modifier.weight(1f)
                        )
                        InsightCard(
                            title = "Книг открыто",
                            value = formatNumber(state.allTime.bookCount.toLong()),
                            icon = Icons.AutoMirrored.Filled.MenuBook,
                            color = StatsGreen,
                            modifier = Modifier.weight(1f)
                        )
                    }
                }

                item { SectionTitle("Библиотека", Icons.AutoMirrored.Filled.MenuBook) }
                item { LibraryCard(state) }

                if (state.recentSessions.isNotEmpty()) {
                    item { SectionTitle("Последние чтения", Icons.Default.History) }
                    itemsIndexed(
                        items = state.recentSessions.take(5),
                        key = { _, session -> session.id }
                    ) { _, session ->
                        RecentSessionCard(session)
                    }
                }

                if (state.ignoredSessionCount > 0) {'''
          stats.write_text(replace_once(text, old, new, "stats most-read section"), encoding="utf-8")
          PY
          git diff --check

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Restore signing key
        id: signing
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_DEBUG_KEYSTORE_BASE64 }}
        run: |
          test -n "$KEYSTORE_BASE64"
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/lumina-update.keystore"
          chmod 600 "$RUNNER_TEMP/lumina-update.keystore"

      - name: Set release version
        run: |
          echo "VERSION_NAME=1.2.${GITHUB_RUN_NUMBER}" >> "$GITHUB_ENV"
          echo "VERSION_CODE=$((200000 + GITHUB_RUN_NUMBER))" >> "$GITHUB_ENV"

      - name: Test and build APK
        id: build
        shell: bash
        run: |
          set -o pipefail
          chmod +x gradlew
          ./gradlew testDebugUnitTest assembleDebug \
            -PversionCodeOverride="$VERSION_CODE" \
            -PversionNameOverride="$VERSION_NAME" \
            -PsigningStoreFile="$RUNNER_TEMP/lumina-update.keystore" \
            -PsigningStorePassword=android \
            -PsigningKeyAlias=AndroidDebugKey \
            -PsigningKeyPassword=android \
            --no-daemon 2>&1 | tee /tmp/reader-stats-hotfix-build.log

      - name: Verify signing certificate
        id: certificate
        env:
          EXPECTED_CERT_SHA256: 646f6722051e0d181095efa4a0166f74adb1ff1eb588d1ca06965bd18a618f51
        run: |
          ACTUAL_CERT_SHA256="$("$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --print-certs app/build/outputs/apk/debug/app-debug.apk | sed -n 's/^Signer #1 certificate SHA-256 digest: //p')"
          test "$ACTUAL_CERT_SHA256" = "$EXPECTED_CERT_SHA256"

      - name: Publish release
        id: publish
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          APK="LuminaReader-${VERSION_NAME}.apk"
          cp app/build/outputs/apk/debug/app-debug.apk "$APK"
          sha256sum "$APK" > "$APK.sha256"
          if gh release view "v${VERSION_NAME}" >/dev/null 2>&1; then
            gh release upload "v${VERSION_NAME}" "$APK" "$APK.sha256" --clobber
          else
            gh release create "v${VERSION_NAME}" "$APK" "$APK.sha256" \
              --title "Lumina Reader ${VERSION_NAME}" \
              --generate-notes \
              --latest
          fi

      - name: Persist validated source in main
        id: persist
        shell: bash
        run: |
          git add \
            app/src/main/java/com/lumina/reader/ui/reader/ReaderScreen.kt \
            app/src/main/java/com/lumina/reader/ui/stats/StatsScreen.kt
          test -n "$(git diff --cached --name-only)"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Show reader status bar and refresh statistics [skip ci]"
          git push origin HEAD:main
          git rev-parse HEAD > /tmp/main-source-sha.txt

      - name: Record success result
        if: success()
        shell: bash
        run: |
          {
            echo "status=success"
            echo "version=$VERSION_NAME"
            echo "main_source_sha=$(cat /tmp/main-source-sha.txt)"
          } > /tmp/reader-stats-hotfix-result.txt
          git reset --hard HEAD
          git clean -fd
          git checkout -B automation/reader-stats-hotfix-result
          cp /tmp/reader-stats-hotfix-result.txt reader-stats-hotfix-result.txt
          git add reader-stats-hotfix-result.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Record successful reader stats deployment"
          git push origin HEAD:refs/heads/automation/reader-stats-hotfix-result --force

      - name: Record failure result
        if: failure()
        shell: bash
        env:
          SIGNING_OUTCOME: ${{ steps.signing.outcome }}
          BUILD_OUTCOME: ${{ steps.build.outcome }}
          CERT_OUTCOME: ${{ steps.certificate.outcome }}
          PUBLISH_OUTCOME: ${{ steps.publish.outcome }}
          PERSIST_OUTCOME: ${{ steps.persist.outcome }}
        run: |
          {
            echo "status=failure"
            echo "signing=$SIGNING_OUTCOME"
            echo "build=$BUILD_OUTCOME"
            echo "certificate=$CERT_OUTCOME"
            echo "publish=$PUBLISH_OUTCOME"
            echo "persist=$PERSIST_OUTCOME"
            echo
            if [ -f /tmp/reader-stats-hotfix-build.log ]; then
              tail -n 500 /tmp/reader-stats-hotfix-build.log
            fi
          } > /tmp/reader-stats-hotfix-result.txt
          git reset --hard HEAD
          git clean -fd
          git checkout -B automation/reader-stats-hotfix-result
          cp /tmp/reader-stats-hotfix-result.txt reader-stats-hotfix-result.txt
          git add reader-stats-hotfix-result.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Record failed reader stats deployment"
          git push origin HEAD:refs/heads/automation/reader-stats-hotfix-result --force
